$('#new_written_apply_modal').remove();
$('#edit_written_apply_modal').remove();
$('body').append('<div class="ui modal" id="edit_written_apply_modal"><i class="close icon"></i><%= j(render 'edit') %></div>');
$('#edit_written_apply_modal').modal('setting', {autofocus: false}).modal('show');
$('select.dropdown').dropdown({placeholder: false});
$("input").focus(function(){
  $(this).parent().removeClass('error');
  $(this).parent().find('.red.pointing').remove();
});